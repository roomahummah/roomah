[build]
  command = "npm install --legacy-peer-deps --include=dev && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  NEXT_USE_NETLIFY_EDGE = "false"
  NEXT_FORCE_EDGE_RUNTIME = "false"

# Force non-www (important for cookie consistency)
[[redirects]]
  from = "https://www.roomahapp.com/*"
  to = "https://roomahapp.com/:splat"
  status = 301
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Environment variables hints (set these in Netlify dashboard)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=your_midtrans_client_key
# MIDTRANS_SERVER_KEY=your_midtrans_server_key

